#include <iostream>
#include <fstream>
using namespace std;

void multiply(int* data, int n) {
    for (int i = 0; i < n; i++) {
        data[i] *= 5;
    }
}

void square(float* data, int n) {
    for (int i = 0; i < n; i++) {
        data[i] = data[i] * data[i];
    }
}

void All() {
    int num[4] = { 1, 2, 3, 4 };
    int int_data[5] = { 1000, 2000, 3000, 4000, 5000 };
    float float_data[3];
    for (int i = 0; i < 3; ++i) {
        float_data[i] = -0.5 + i * 0.5;
    }

    multiply(int_data, 5);
    square(float_data, 3);

    ofstream xx("mybin.dat", ios::binary | ios::out);
    if (!xx) cout << "Fail" << endl;

    xx.write((char*)(&num), 4 * sizeof(int));
    xx.write((char*)(&int_data), 5 * sizeof(int));
    xx.write((char*)(&float_data), 3 * sizeof(float));

    xx.close();

    ifstream yy("mybin.dat", ios::binary | ios::in);
    if (!yy) cout << "Fail" << endl;

    yy.read((char*)(&num), sizeof(num));
    cout << "num: ";
    for (int i = 0; i < 4; ++i) {
        cout << " " << num[i];
    }
    cout << endl;

    yy.read((char*)(&int_data), sizeof(int_data));
    cout << "int_data:";
    for (int i = 0; i < 5; ++i) {
        cout << " " << int_data[i];
    }
    cout << endl;

    yy.read((char*)(&float_data), sizeof(float_data));
    cout << "float_data:";
    for (int i = 0; i < 3; ++i) {
        cout << " " << float_data[i];
    }
    cout << endl;

    yy.close();


}

int main() {
    All();
    return 1;
}
